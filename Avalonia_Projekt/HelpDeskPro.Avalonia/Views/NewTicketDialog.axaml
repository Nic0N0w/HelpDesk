<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="HelpDeskPro.Avalonia.Views.NewTicketDialog"
        Title="Neues Ticket erstellen"
        Height="450" Width="460"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Neues Ticket erstellen" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,16"/>

        <Border x:Name="ErrorBorder" Grid.Row="1" Background="#FFF0F0" BorderBrush="#FFCCCC"
                BorderThickness="1" CornerRadius="6" Padding="10,6" Margin="0,0,0,10"
                IsVisible="False">
            <TextBlock x:Name="ErrorText" Foreground="#C0392B" FontSize="12"/>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Titel *" FontSize="12" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,5"/>
            <TextBox x:Name="TitleBox" Classes="Modern"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Beschreibung" FontSize="12" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,5"/>
            <TextBox x:Name="DescBox" Classes="Modern"
                     AcceptsReturn="True" Height="80"
                     TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="Priorität" FontSize="12" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,5"/>
            <ComboBox x:Name="PriorityBox" Classes="Modern" SelectedIndex="1">
                <ComboBoxItem Content="Low"/>
                <ComboBoxItem Content="Medium"/>
                <ComboBoxItem Content="High"/>
                <ComboBoxItem Content="Critical"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Abbrechen" Classes="Secondary"
                    Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Erstellen" Classes="Primary"
                    Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
