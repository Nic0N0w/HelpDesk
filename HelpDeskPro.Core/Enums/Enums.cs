namespace HelpDeskPro.Core.Enums;

public enum TicketStatus
{
    Open,
    InProgress,
    Closed
}

public enum Priority
{
    Low,
    Medium,
    High,
    Critical
}

public enum UserRole
{
    Employee,
    Admin
}
